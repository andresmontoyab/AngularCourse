// Creacion de una funciona tradicional en javaScript
let log = function(message) {
    console.log(message);
} 

// Creacion de un funcion Arrow
let doLog = (message) => {
    console.log(message);
}

//Creacion de una funcion arrow más corta.
let doLogShorter = (message) => console.log(message);